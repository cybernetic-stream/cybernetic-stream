apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: efs-pvc
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 500Gi # Ensure this is less than or equal to the PV capacity
  volumeName: filestore-pv # This binds your PVC to the specific PV created above
